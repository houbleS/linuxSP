#include <unistd.h>
#define _XOPEN_SOURCE 500

int main()
{
  /* ssize_t pread(int fd, void *buf, size_t count, off_t pos); */
  /* ssize_t pwrite(int fd, const void *buf, size_t coint, off_t pos) */
  
  return 0;
}