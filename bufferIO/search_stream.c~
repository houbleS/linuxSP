#include <stdio.h>

int main()
{
  /* int fsee(FILE *stream, long offset, int whence); 
     whence : SEEK_SET, SEEK_CUR, SEEK_END
   */
  /* int fsetpos(FILE *stream, fpos_t *pos); */
  
  return 0;
}
